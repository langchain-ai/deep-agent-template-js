{
  "name": "deep-agent-template",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "langgraphjs dev",
    "build": "langgraphjs build",
    "start": "langgraphjs run agent",
    "test": "vitest run",
    "test:integration": "vitest run tests/integration --runInBand",
    "lint": "prettier --check src tests",
    "format": "prettier --write src tests"
  },
  "dependencies": {
    "@langchain/core": "^1.1.32",
    "deepagents": "^1.8.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "vitest": "^1.6.0"
  }
}
